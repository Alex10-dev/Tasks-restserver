# Tasks App RestServer (Project)

## Installation

Use the following commands:

```bash
npm i
```

Don't forget to recreate the following files:
    - `.env`

Start the PostgreSQL database container:

```bash
docker compose up -d
```

Run the server in development mode:

```bash
npm run dev
```